# Development data

This directory contains all 21 labeled Industry Documents examples currently used for development.
There is no separate test split yet.

`ground_truth.json` maps each document ID to its document-scoped list of canonical `PIIItem` values.
