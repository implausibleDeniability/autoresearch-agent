# Debug data

This directory contains two complete Industry Documents examples for inexpensive pipeline checks.
One document contains six labeled people and 18 labeled PII values; the other is a negative example.
Together they contain fewer than 1,000 source tokens.

`ground_truth.json` maps each document ID to its document-scoped list of canonical `PIIItem` values.
